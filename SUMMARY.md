# 项目总结与设计说明

## 项目定位
这是一个基于 CMake 的多项目工作区（workspace）示例，核心目标是：
1. 统一各子项目的构建约定（输出目录、构建类型、安装与导出规则）。
2. 将“新增项目”流程标准化成可复用模板（命令行引导 + 预设文件）。
3. 保持增量编译最小化，避免 glob 导致的重复配置和不必要的重编译。

## 目录结构（关键）
- `cmake/`：自定义 CMake 辅助模块与命令行模板生成器。
- `projects/`：工作区内的实际子项目。
- `CMakeLists.txt`：工作区入口，聚合子项目并启用通用配置。
- `config.md`：整理了 `cmake/` 中用户可配置变量与类型。
- `preset.json`：首次引导保存的模板配置（由 CLI 生成）。

## 关键模块与“为什么这样做”

### 1) `AddTarget.cmake`
**做什么：** 统一创建库或可执行目标，包含默认编译特性、包含目录、安装与导出、CTest 注册等。
**为什么这样做：**
- 明确要求 `DIR_SOURCES` 显式列出，避免 `file(GLOB ...)` 触发重新配置，保证最小化重编译。
- 将常用的“是否安装、是否注册测试、是否导出”做成可选变量，降低每个子项目重复配置成本。

### 2) `AddWorkspaces.cmake`
**做什么：** 负责把 `projects/` 下的子项目加入构建。
**为什么这样做：**
- 支持 `DIRS` 显式列表，避免扫描/递归 glob，引导可控、可复现。
- 保留 `FIRST|ALL|LEAF` 等模式以便在需要扫描时灵活筛选。

### 3) `BuildType.cmake`
**做什么：** 统一单配置与多配置生成器的构建类型处理。
**为什么这样做：**
- 避免不同生成器下 `CMAKE_BUILD_TYPE` 与 `CMAKE_CONFIGURATION_TYPES` 行为不一致导致的误用。
- 给出默认构建类型，减少新用户配置成本。

### 4) `UnifiedOutputDirs.cmake`
**做什么：** 统一二进制与库输出目录。
**为什么这样做：**
- 让所有项目输出结构一致，便于打包、部署和 CI。

### 5) `SetupPackage.cmake`
**做什么：** 生成并安装 CMake Package 配置，支持 `find_package`。
**为什么这样做：**
- 为库项目提供标准化的导出方式，方便被其他项目复用。

### 6) `AddUninstall.cmake` + `cmake_uninstall.cmake.in`
**做什么：** 提供 `uninstall` 目标，删除已安装文件。
**为什么这样做：**
- CMake 默认不带卸载目标，补齐开发体验。

### 7) `DeployPackage.cmake`
**做什么：** 将构建产物与必要文件打包为 zip/tar.gz。
**为什么这样做：**
- 提供一键交付能力，降低手工打包风险。

### 8) `cmake/cli.py`（引导式模板生成器）
**做什么：** 类似 Vue CLI 的交互式创建流程，生成可复用的项目骨架，并保存 `preset.json`。
**为什么这样做：**
- 降低“新增子项目”的门槛，保持统一的工程结构与 CMake 约定。
- `preset.json` 使创建流程可复用、可自动化。

## 工作流（怎么用）
1. 运行 `python cmake/cli.py` 进入交互式引导。
2. 首次引导会保存 `preset.json`，之后可用 `--preset preset.json` 直接生成。
3. 生成后会自动加入根 `CMakeLists.txt` 的 `DIRS` 列表。
4. 正常执行 CMake 配置与构建即可。

## 设计取舍与影响
- **显式源文件列表**：减少不必要的重新配置，增量编译更可靠；代价是新增文件需要手动或通过 CLI 更新列表。
- **DIRS 显式列表**：工作区结构更可控，避免扫描误命中；代价是新增项目需要更新根入口（CLI 已自动处理）。
- **统一模块化封装**：提高一致性与可维护性；代价是对新手需要熟悉 `DIR_*` 和 `SP_*` 等变量。

## 参考与扩展
如需更详尽的可配置变量列表，请查看 `config.md`，其中已整理 `cmake/` 模块的可配置项。
